# Кондратюк Егор 3ИСд-1.22
# Практическая №12 Вариант 15
# Множество содержит п целых случайных равномерно распределенных чисел из интервала [a; b]. Требуется:
# а) напечатать все элементы множества, являющиеся простыми числами;
# б) определить, сколько в данном множестве четных чисел из интервала [с; d];
# в) определить, сколько элементов содержится в этом множестве;
# г) определить, сколько чисел из интервала [а; b] не попало в данное множество;
#д) разбить полученное множество на два: множество, содержащее числа, в двоичной записи которых более k нулей, и множество, содержащее все остальные числа.





import random

# Функция для проверки простого числа
def is_prime(n):
    if n < 2:
        return False
    for i in range(2, int(n**0.5) + 1):
        if n % i == 0:
            return False
    return True

# Функция для подсчёта количества нулей в двоичной записи числа
def count_zeros_in_binary(n):
    return bin(n).count('0') - 1  # Вычитаем 1, чтобы не учитывать '0' в начале

# Ввод данных
n = int(input("Введите количество случайных чисел (n): "))
a, b = map(int, input("Введите границы диапазона случайных чисел [a, b]: ").split())
c, d = map(int, input("Введите границы диапазона для поиска чётных чисел [c, d]: ").split())
k = int(input("Введите минимальное количество нулей в двоичной записи (k): "))

# Генерация множества случайных чисел в диапазоне [a, b]
random_numbers_set = set(random.randint(a, b) for _ in range(n))

# Формирование результатов с понятными названиями
results = {
    "Простые числа в множестве": [num for num in random_numbers_set if is_prime(num)],
    "Чётные числа в диапазоне [c, d]": [num for num in random_numbers_set if c <= num <= d and num % 2 == 0],
    "Общее количество элементов в множестве": len(random_numbers_set),
    "Чисел из диапазона [a, b], которые не попали в множество": (b - a + 1) - len(random_numbers_set),
    "Числа с более чем k нулями в двоичной записи": {num for num in random_numbers_set if count_zeros_in_binary(num) > k},
    "Оставшиеся числа": {num for num in random_numbers_set if count_zeros_in_binary(num) <= k}
}

# Вывод результатов
print("\n🔹 Результаты анализа множества:")
for description, value in results.items():
    print(f"{description}: {value}")
